# Leave DATABASE_URL empty for local development: games are stored as JSON files
# under .data/ so you can play without setting up any database.
#
# In production (Vercel) set DATABASE_URL to your Neon connection string and the
# app switches to PostgreSQL automatically. Nothing else changes.
DATABASE_URL=
